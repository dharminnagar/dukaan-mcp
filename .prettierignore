node_modules
bun.lock
dist
build
web/.next
# Derived from events.jsonl or hand-authored prose; reformatting either would
# create noise in an audit trail that ships publicly.
.projectmem
# Generated, byte-identical-by-contract eval dataset. Reformatting it would
# change the file the determinism test compares against.
fixtures/eval

# Hand-authored standalone HTML checklist with inline CSS and JS. Prettier
# rewrote 2121 lines of it for no gain and every line was review noise.
setup

# CLAUDE.md carries a projectmem bridge block that `pjm export` regenerates.
# Formatting machine-managed content just guarantees churn on the next export.
CLAUDE.md

# eslint --cache artifact
.eslintcache


# Machine-local permission state written by Claude Code. Git-ignored globally and
# never committed, but Prettier's ignore list is separate from git's, so without
# this line `bun run lint` fails on a file that only exists on one machine.
.claude/settings.local.json
