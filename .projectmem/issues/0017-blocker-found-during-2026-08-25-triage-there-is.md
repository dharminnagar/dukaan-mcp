# #0017 BLOCKER found during 2026-08-25 triage: there is no ANTHROPIC_API_KEY in .env (only DATABASE_URL, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET) and @anthropic-ai/sdk is not installed. This blocks two things and one of them is the highest-risk ticket in the project. (1) DUK-17, the Claude-API buyer agent, cannot start at all. Its own ticket calls it 'the single task that kills the submission if it goes wrong' and carries a hard gate of 21:00 on 29 Aug, with four deliverables hanging off it: the middle of the 5-minute video, the 'one failure handled gracefully' requirement, the MCP client wiring claim, and the hero demo session. There is no recovery window after it. (2) The half of DUK-18 that generates adversarial transcripts from a SEPARATE Claude call whose context holds only the tool schema and the policy JSON, never the gate implementation. That generator is the fix for issue #0010's circularity problem and is the honesty differentiator the whole measurement story leans on; without it every adversarial transcript is authored by the same person who wrote the rules, which is exactly the weakness the README is supposed to disclose rather than embody. MITIGATION IN FLIGHT: DUK-18's deterministic half is being built now without any API dependency, because the eval drives decide() directly and runs on scripted transcripts by design. Every transcript is labelled origin:'hand' so a later LLM-generated batch is distinguishable in the data and the README can state honestly which attacks were self-authored. A TranscriptSource seam is being left so the generator plugs in without touching the runner or the metrics. ACTION REQUIRED FROM DHARMIN: add ANTHROPIC_API_KEY to .env and install @anthropic-ai/sdk before D7. This is the only item on the board with no workaround.

- 2026-08-25T05:04:57Z `issue`: BLOCKER found during 2026-08-25 triage: there is no ANTHROPIC_API_KEY in .env (only DATABASE_URL, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET) and @anthropic-ai/sdk is not installed. This blocks two things and one of them is the highest-risk ticket in the project. (1) DUK-17, the Claude-API buyer agent, cannot start at all. Its own ticket calls it 'the single task that kills the submission if it goes wrong' and carries a hard gate of 21:00 on 29 Aug, with four deliverables hanging off it: the middle of the 5-minute video, the 'one failure handled gracefully' requirement, the MCP client wiring claim, and the hero demo session. There is no recovery window after it. (2) The half of DUK-18 that generates adversarial transcripts from a SEPARATE Claude call whose context holds only the tool schema and the policy JSON, never the gate implementation. That generator is the fix for issue #0010's circularity problem and is the honesty differentiator the whole measurement story leans on; without it every adversarial transcript is authored by the same person who wrote the rules, which is exactly the weakness the README is supposed to disclose rather than embody. MITIGATION IN FLIGHT: DUK-18's deterministic half is being built now without any API dependency, because the eval drives decide() directly and runs on scripted transcripts by design. Every transcript is labelled origin:'hand' so a later LLM-generated batch is distinguishable in the data and the README can state honestly which attacks were self-authored. A TranscriptSource seam is being left so the generator plugs in without touching the runner or the metrics. ACTION REQUIRED FROM DHARMIN: add ANTHROPIC_API_KEY to .env and install @anthropic-ai/sdk before D7. This is the only item on the board with no workaround. [.env]
- 2026-08-25T06:27:11Z `fix`: Issue #0017 resolved differently than expected: Dharmin supplied OPENROUTER_API_KEY instead of ANTHROPIC_API_KEY, to be used with the model he called '0xAlpha'. Verified against OpenRouter's live model list: the actual slug is stealth/ox-alpha (letter O, not a zero — 'Ox Alpha'), and it meets the hard requirements. supported_parameters includes 'tools' and 'tool_choice', so MCP tool calling works, which is the one capability DUK-17's buyer agent cannot do without. context_length 1048576, max_completion_tokens 131072, reasoning and reasoning_effort supported, pricing 0/0. Description: 'a reasoning model designed for coding, sustained agentic work, and production workloads'. I could NOT verify the claim that it is Sonnet-level; nothing in the listing supports or refutes that, and it does not matter as long as tool calling and context suffice, which they do. THREE RISKS TO CARRY. (1) It is a STEALTH model. Cloaked/stealth slugs on OpenRouter are anonymous temporary previews that get renamed or withdrawn with little notice, and the submission deadline is ~5 Sept. Building the highest-risk component on a slug that may vanish is a real schedule risk. Mitigation: OpenRouter's API is OpenAI-compatible, so the client must be provider-agnostic with base URL and model id read from env, making a swap a one-variable change rather than a rewrite. (2) Free pricing on a stealth model generally means the provider is collecting evaluation data. Acceptable here — no real customer data, Razorpay test mode only — but it should be a conscious acceptance, not an accident. (3) DOCUMENTATION CONSEQUENCE, and this one is not optional: IDEA.md currently says 'A Claude-API buyer agent', its trade-off table says 'Buyer agent: Custom Claude-API MCP client', and the DUK-18 circularity fix is described as 'a separate Claude call'. All three become factually wrong. The video and README must say OpenRouter and name the model, or a judge who opens the repo finds a mismatch with the pitch — which is exactly the kind of small inaccuracy that discredits an otherwise scrupulous submission. [.env]
