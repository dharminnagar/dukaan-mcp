# #0004 Two hard Razorpay API constraints found. (1) Payment Links are capped at 30 per business in TEST MODE — a checkout flow that mints a Payment Link per order will exhaust this across dev + demo + retries. Use the Orders API as the primary rail. (2) UPI Reserve Pay is NOT buildable: no public API reference, no test-mode sandbox, closed pilot only. The IDEA.md trade-off row citing Reserve Pay must be dropped or explicitly labelled future work — a Razorpay panel will know it is gated.

- 2026-08-22T15:36:39Z `issue`: Two hard Razorpay API constraints found. (1) Payment Links are capped at 30 per business in TEST MODE — a checkout flow that mints a Payment Link per order will exhaust this across dev + demo + retries. Use the Orders API as the primary rail. (2) UPI Reserve Pay is NOT buildable: no public API reference, no test-mode sandbox, closed pilot only. The IDEA.md trade-off row citing Reserve Pay must be dropped or explicitly labelled future work — a Razorpay panel will know it is gated.
- 2026-08-25T11:32:01Z `fix`: RESOLVED by two recorded decisions. On the Payment Links cap: the Orders API is the primary rail, POST /v1/orders, confirmed working with two real test-mode orders (order_TTgT79PWPuu8Fh from the smoke script and order_TTgb8dEARAHPVv through the adapter). Payment Links never touch the happy path, and DUK-16's ticket forbids putting them back even for convenience. On UPI Reserve Pay: it is dropped from every claim and appears only as explicitly labelled future work in the trade-off table, with the rationale that it has no public API reference, no test-mode sandbox and is a closed pilot, so citing it as part of the build would mean claiming something unbuildable. The rate-limit half is also settled: Razorpay publishes no numeric limits, only that a limiter exists and that 429 should be handled with randomized exponential backoff, which src/razorpay/index.ts implements with full jitter capped at one retry.
