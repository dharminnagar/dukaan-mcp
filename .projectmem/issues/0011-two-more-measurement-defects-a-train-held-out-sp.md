# #0011 Two more measurement defects. (a) Train/held-out splits detect overfitting of FITTED PARAMETERS; hand-written rules have none, so the ML vocabulary implies a rigor that is not present and reads as a tell. Reframe as pre-registration and hold-out discipline: the training split is the only data looked at while tuning, the held-out split runs exactly ONCE at the end, and the README states the split was frozen before tuning. (b) False-positive cost in rupees is not defensible as 'the order total that would have been real revenue': the totals come from the builder's own seeded price distribution, blocked GMV is not lost revenue when the graceful-failure story has the agent re-plan and substitute, and GMV/merchant margin/Razorpay MDR are three different numbers. Restate as blocked benign GMV at the seeded price distribution, explicitly an upper bound, alongside a recovery rate and a net figure, and publish the price distribution so a reader can rescale.

- 2026-08-23T05:17:42Z `issue`: Two more measurement defects. (a) Train/held-out splits detect overfitting of FITTED PARAMETERS; hand-written rules have none, so the ML vocabulary implies a rigor that is not present and reads as a tell. Reframe as pre-registration and hold-out discipline: the training split is the only data looked at while tuning, the held-out split runs exactly ONCE at the end, and the README states the split was frozen before tuning. (b) False-positive cost in rupees is not defensible as 'the order total that would have been real revenue': the totals come from the builder's own seeded price distribution, blocked GMV is not lost revenue when the graceful-failure story has the agent re-plan and substitute, and GMV/merchant margin/Razorpay MDR are three different numbers. Restate as blocked benign GMV at the seeded price distribution, explicitly an upper bound, alongside a recovery rate and a net figure, and publish the price distribution so a reader can rescale.
- 2026-08-25T11:32:51Z `fix`: RESOLVED, both measurement defects, by DUK-19's reporter. On the ML-vocabulary defect: the report explicitly refuses precision and recall in its own text — 'This is coverage over a declared threat model, not precision or recall: the gate is hand-written thresholds and allowlists with no fitted parameters, so borrowed ML evaluation vocabulary would claim a rigor this system does not have.' The train/held-out split is reframed as pre-registration and hold-out discipline rather than overfitting control, the split is stratified per attack class and frozen with a recorded timestamp and note, and the held-out run happens once at DUK-20 with a loud banner warning if the flag is passed early. On the false-positive-cost defect: no single lost-revenue number appears anywhere. The report gives three figures together — blocked benign GMV explicitly labelled an UPPER BOUND with the reasoning printed inline, a recovery rate, and a net — and states in the output that merchant GMV, merchant margin and Razorpay MDR are three different numbers and this figure is none of them. The seeded price distribution is published in the report so a reader can rescale, and per-SKU catalog price is kept visibly distinct from per-order basket value. Recovery is reported NOT MEASURABLE and net NOT COMPUTABLE rather than assuming zero, which is the same discipline this issue was asking for. A separate finding remains open about the cost figure having no data yet because the benign corpus contains no near-misses; that is a corpus gap, not the framing defect this issue described.
