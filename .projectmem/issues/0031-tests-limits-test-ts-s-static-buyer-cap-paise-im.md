# #0031 tests/limits.test.ts's static "buyer_cap_paise immutability" guard false-positives on rotateAgentToken's UPDATE agents ... RETURNING id, merchant_id, buyer_cap_paise — the regex only checks for the UPDATE-agents statement text containing buyer_cap_paise, so a RETURNING clause that reads the column trips it even though nothing SETs it

- 2026-09-02T10:21:23Z `issue`: tests/limits.test.ts's static "buyer_cap_paise immutability" guard false-positives on rotateAgentToken's UPDATE agents ... RETURNING id, merchant_id, buyer_cap_paise — the regex only checks for the UPDATE-agents statement text containing buyer_cap_paise, so a RETURNING clause that reads the column trips it even though nothing SETs it [src/buyer/provision.ts, tests/limits.test.ts]
- 2026-09-02T10:23:41Z `fix`: Narrowed the buyer_cap_paise-immutability guard in tests/limits.test.ts to only scan the SET clause of each UPDATE agents statement, not the whole statement — it was flagging rotateAgentToken's harmless RETURNING id, merchant_id, buyer_cap_paise as a mutation. Verified: only src/buyer/provision.ts has an UPDATE agents statement in src/, and the guard still correctly requires the offenders list to be empty.
