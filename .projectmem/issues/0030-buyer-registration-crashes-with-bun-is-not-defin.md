# #0030 Buyer registration crashes with "Bun is not defined" in the web app. src/buyer/auth.ts calls Bun.password.hash/verify, but web/ runs Next under real Node (next dev's binary resolves node via its shebang even when launched through `bun run dev`), so the Bun global does not exist there. bun test passes because tests run under a real bun process, masking the bug.

- 2026-09-02T05:25:45Z `issue`: Buyer registration crashes with "Bun is not defined" in the web app. src/buyer/auth.ts calls Bun.password.hash/verify, but web/ runs Next under real Node (next dev's binary resolves node via its shebang even when launched through `bun run dev`), so the Bun global does not exist there. bun test passes because tests run under a real bun process, masking the bug. [src/buyer/auth.ts]
- 2026-09-02T05:28:32Z `fix`: Replaced Bun.password.hash/verify in registerBuyer/loginBuyer with node:crypto scryptSync (N=16384,r=8,p=1, 64-byte key, self-describing scrypt$N$r$p$salt$hash string), verified via timingSafeEqual — fixes crash under Next's Node-shebang server process. [src/buyer/auth.ts]
